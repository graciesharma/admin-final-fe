import { createStyles } from "@mantine/core";

export const useTableStyles = createStyles(() => ({
  row: {
    marginTop: "10px",
    marginBottom: "10px",
  },
  td: {
    padding: "0 1rem 0 1rem",
  },
}));
